# Base URL of the Pulse API. Leave empty in local dev to use the Vite proxy
# (which forwards /api to http://127.0.0.1:8000). In production set this to the
# deployed backend, e.g. https://pulse-api.onrender.com
VITE_API_BASE=
